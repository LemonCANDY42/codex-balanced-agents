# 卸载 Codex Balanced Agents

[English](UNINSTALL.md) · [README](README.zh-CN.md)

## 一条命令卸载

在已检查的最新仓库目录中执行，需要 Python 3.11+：

```bash
python3 install.py uninstall --yes
```

这只跳过确认提问，**不会跳过归属或完整性检查**。不需要 Codex 可执行程序、账号、模型访问权限或网络连接。Windows 可将 `python3` 换成 `py -3.11`。

目标目录优先使用 `$CODEX_HOME`，否则为 `~/.codex`。如果安装时指定了自定义目录，卸载时应使用同一目录：

```bash
python3 install.py uninstall --codex-home /path/to/codex-home --dry-run
python3 install.py uninstall --codex-home /path/to/codex-home --yes
```

`--dry-run` 检查并列出确切删除路径，不修改文件，也不调用 Codex。省略 `--yes` 则先显示清单再交互确认。卸载前结束活跃任务，期间不要同时运行其他安装器或修改受管理文件。完成后重新启动 Codex 客户端，让角色与技能发现结果刷新。

## 给 agent 的执行步骤

1. 使用本仓库的干净副本并检查 `install.py`，报告所用提交。如果原仓库已删除，克隆到新的临时目录，不覆盖已有文件。不要为了卸载而安装或升级 Codex。
2. 确认安装时使用的 Codex 目录。目标不明确时，先澄清再删除；不要扫描并删除其他目录或项目中的同名文件。
3. 先执行 `python3 install.py uninstall --codex-home <home> --dry-run`，检查目标清单。用户已授权卸载且目录正确时，将 `--dry-run` 换为 `--yes` 执行，无需重复确认。
4. 执行 `python3 install.py status --codex-home <home>`，报告删除的文件、保留的本机状态和冲突，并提醒重启 Codex。命令失败时不得声称卸载成功。
5. 发生冲突就停止并解释。不得通过递归删除、修改哈希、删除 manifest、还原用户定制或删除无关配置来强行完成。

## 删除边界

- 文件必须同时在本机归属清单中，且属于当前仓库的 14 个角色路径或 `skills/codex-balanced-agents/SKILL.md`。清单外文件即使内容相同，也不会被接管。
- 清单中的每个文件必须存在、是普通文件，且 SHA-256 与记录一致。文件被修改或缺失、清单损坏、目标目录或文件为符号链接时，预检会在删除前停止。清单即使附有正确哈希，也不能指定无关角色。
- 删除过程中重新检查目标目录和归属清单，每个文件在删除前再次校验。不会递归删除 `agents/`、`skills/` 或 Codex 根目录；项目技能目录仅在为空时移除。
- 保留主 `config.toml`、全局 `AGENTS.md`、认证、MCP 设置、其他角色／技能及用户额外文件。本机备份和停用状态的归属清单也会保留，用于记录卸载结果；它们不是活跃角色，不会自动清理。
- 捕获到错误后，仅尝试恢复本次确实删除的文件，使用排他创建避免覆盖后来出现的新文件；恢复不完整会明确报错。这不是抗断电事务，也不能防御恶意并发修改文件系统；请勿同时编辑文件。
- 成功后重复卸载不会再改动。手动复制安装的文件没有归属记录，不会被自动删除。如果额外文件使项目技能目录在卸载后仍存在，后续安装会报告非托管目录冲突；请明确处理，不要直接删除整个目录。

若希望保留定制角色，请先另存，再决定如何处理冲突。卸载器不会猜测哪些改动应保留，也不要删除归属清单来绕过检查。
